{
  "name": "terrain-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for terrain elevation data",
  "main": "worker.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@sentry/cloudflare": "^9.32.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
